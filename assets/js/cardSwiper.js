var cardSwiper={
    // Swiper Config
        swipeType: "linear",     // or Circular
        lookUpCriteria: "id",    // or class
    
    // Swiper Globals { don't touch :) }
        current:,
        numberOfCards:,
        cardsLookup:[],
    
    // Swiper Functions
        
        /* 
        - Parses page HTML looking for cards with class name 'card-swipable'
        - Unhides the first card.
        - Adds custom ID or Class (look Swiper Config) to cards.
        - Updates numberOfCards & cardsLookup variables.
        */
        updateNumberOfCards: function(){
            
        }
    
};